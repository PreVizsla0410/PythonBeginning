#Ein Tuple ist eine geordnete, sequenzielle Sammlung. Es kann Daten aller Typen aufnehmen, die über einen Index erreichbar sind. Ein Tuple kann im Gegensatz zu einer Liste nicht geändert werden. Sobald ein Element in das Tuple eingefügt wurde, kann es nicht mehr verändert oder gelöscht werden.

three_numbers = (1, 2 , 3); # es müssen normale Klammern sein
#three_numbers[1] = 23; - das wird nicht funktionieren, da Tuples Konstante sind und keine Variablen
#print(three_numbers[0]);

#print(three_numbers[0]); - So kann man den ersten Wert rausfinden

#print(len(three_numbers)); - zeigt mir wie viele Werte in dieser Tuple sind

#Tuples are for coordinates or similarr things - things that shouldn´ t be changed
